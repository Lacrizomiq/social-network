<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Home</h1>
    <ul>
      <% users.forEach(user => { %>
      <li><%= user.username %></li>
      <li><%= user.email %></li>
      <li><%= user.bio %></li>
      <li><%= user.profile_picture %></li>
      <% }) %>
    </ul>
  </body>
</html>
